# Lab 01: Simple Drawing

## Description

This project uses Python Turtle to draw the Qatar flag by reading drawing instructions from a text file. It also implements a simple singly linked list.

## Files

- `Drawing.py` – reads and executes the drawing instructions.
- `QatarFlag.txt` – contains the Qatar flag drawing commands.
- `LinkedList.py` – implements a simple linked list.